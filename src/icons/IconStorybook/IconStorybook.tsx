import { createIcon } from '@consta/uikit/createIcon';

import IconStorybookSizeM from './IconStorybook.svg';

export const IconStorybook = createIcon({
  m: IconStorybookSizeM,
  s: IconStorybookSizeM,
  xs: IconStorybookSizeM,
  name: 'IconStorybook',
});
